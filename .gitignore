# Binaries (built in repo root)
/bundler
/keygen
*.exe

# Test
*.test
coverage.out

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Bundler runtime (never commit)
keystore.json
bundler.db
bundler.db-wal
bundler.db-shm

bundler.toml

# Devnet local state
.devnet/
bin/

# Forge build artifacts
out/
cache/
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/foundry.lock

# Railway IaC: the CLI regenerates railway.ts on some commands. This repo
# authors the Go variant (.railway/railway.go); two files break `config plan`.
.railway/railway.ts
